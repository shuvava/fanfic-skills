# One .env per fic project, at its root — shared by every skill and script (CONVENTIONS.md §12).
# Copy this file to .env there and fill in the values. .env is gitignored; never commit it.
# A variable already set in the shell wins over this file.

# Image generation on OpenRouter — scripts/openrouter_image.py (illustrate, cover)
OPENROUTER_API_KEY=
